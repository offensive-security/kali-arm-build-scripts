kali-support.tar:  kali build scripts for debootstrap, and the kali gpg key.
pine64_eth0-mackeeper.sh:  sets the mac address of eth0 permanently into the pine64 uboot environment
pine64_update_uboot.sh: updates the uboot environment with latest code
